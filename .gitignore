# зависимости и сборка
node_modules/
dist/

# временные/тестовые артефакты
_nouserdir_/
*.log
.cache/

# ОС
.DS_Store
Thumbs.db

# локальные настройки и секреты (в проекте их быть не должно — настройки лежат в userData,
# но перестрахуемся, чтобы случайно не закоммитить ключи/пароли)
settings.json
.env
*.pem
*.key
